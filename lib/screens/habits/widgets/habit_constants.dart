/// Shared dimensions so the column header and mini cells align perfectly.
const double heatmapCellSize = 28.0;
const double heatmapCellGap = 4.0;
const double heatmapCellTotal = heatmapCellSize + heatmapCellGap * 2;
